<!-- ───── مودال ویرایش/افزودن غذا ───── -->
<div id="editModal" class="fixed inset-0 z-[60] hidden">
     <div class="absolute inset-0 bg-black/50 backdrop-blur-sm" onclick="closeEditModal()"></div>
     <div
          class="absolute bottom-0 left-0 right-0 mx-auto w-full max-w-2xl bg-white rounded-t-2xl sm:rounded-t-3xl shadow-2xl transform transition-transform duration-300 translate-y-full overflow-hidden"
          id="editPanel"
          style="max-height: 85vh"
     >
          <div class="flex justify-center pt-2 sm:pt-3 pb-1 sticky top-0 bg-white z-10">
               <div class="w-10 h-1 rounded-full bg-gray-200"></div>
          </div>
          <div
               class="px-4 sm:px-5 pb-5 sm:pb-6 overflow-y-auto"
               style="max-height: calc(85vh - 20px)"
          >
               <h3
                    class="text-base sm:text-[16px] font-extrabold text-gray-800 mb-4 sm:mb-5"
                    id="editModalTitle"
               >
                    ویرایش غذا
               </h3>
               <form id="editForm" class="space-y-3 sm:space-y-4" enctype="multipart/form-data">
                    <input type="hidden" id="editFoodId" name="foodId" />
                    <input type="hidden" id="editCategory" name="category" />

                    <!-- نام غذا -->
                    <div>
                         <label class="block text-[11px] font-bold text-gray-500 mb-1.5 mr-1"
                              >نام غذا</label
                         >
                         <input
                              type="text"
                              id="editName"
                              name="name"
                              required
                              placeholder="مثلاً: پیتزا مخصوص"
                              class="w-full px-4 py-3 bg-cream-50 border-2 border-gray-100 rounded-xl text-[13px] text-gray-700 placeholder-gray-300 outline-none font-medium transition-all duration-300 focus:border-brand-500 focus:shadow-[0_0_0_3px_rgba(249,115,22,.15)]"
                         />
                    </div>

                    <!-- توضیحات -->
                    <div>
                         <label class="block text-[11px] font-bold text-gray-500 mb-1.5 mr-1"
                              >مواد تشکیل‌دهنده</label
                         >
                         <textarea
                              id="editDesc"
                              name="description"
                              rows="2"
                              placeholder="مثلاً: سوسیس، قارچ، فلفل، پنیر"
                              class="w-full px-4 py-3 bg-cream-50 border-2 border-gray-100 rounded-xl text-[13px] text-gray-700 placeholder-gray-300 outline-none font-medium transition-all duration-300 focus:border-brand-500 focus:shadow-[0_0_0_3px_rgba(249,115,22,.15)] resize-none"
                         ></textarea>
                    </div>

                    <!-- قیمت -->
                    <div>
                         <label class="block text-[11px] font-bold text-gray-500 mb-1.5 mr-1"
                              >قیمت (تومان)</label
                         >
                         <input
                              type="text"
                              id="editPrice"
                              name="price"
                              required
                              placeholder="۱۸۵,۰۰۰"
                              class="w-full px-4 py-3 bg-cream-50 border-2 border-gray-100 rounded-xl text-[13px] text-gray-700 placeholder-gray-300 outline-none font-medium transition-all duration-300 focus:border-brand-500 focus:shadow-[0_0_0_3px_rgba(249,115,22,.15)]"
                         />
                    </div>

                    <!-- دسته‌بندی (سفارشی) -->
                    <div class="relative" id="customSelectWrapper">
                         <label class="block text-[11px] font-bold text-gray-500 mb-1.5 mr-1"
                              >دسته‌بندی</label
                         >
                         <!-- دکمه باز کردن -->
                         <button
                              type="button"
                              id="customSelectBtn"
                              onclick="toggleCategoryDropdown()"
                              class="w-full px-4 py-3 bg-cream-50 border-2 border-gray-100 rounded-xl text-[13px] text-gray-700 font-medium transition-all duration-300 text-right flex items-center justify-between focus:border-brand-500 focus:shadow-[0_0_0_3px_rgba(249,115,22,.15)]"
                         >
                              <span
                                   id="selectedCatLabel"
                                   class="flex items-center gap-2 text-gray-300"
                                   >انتخاب دسته‌بندی</span
                              >
                              <svg
                                   class="w-4 h-4 text-gray-400 transition-transform duration-200"
                                   id="dropdownArrow"
                                   viewBox="0 0 24 24"
                                   fill="none"
                                   stroke="currentColor"
                                   stroke-width="2"
                                   stroke-linecap="round"
                                   stroke-linejoin="round"
                              >
                                   <polyline points="6 9 12 15 18 9" />
                              </svg>
                         </button>
                         <!-- لیست -->
                         <div
                              id="categoryDropdown"
                              class="hidden absolute left-0 right-0 mt-1.5 bg-white border-2 border-gray-100 rounded-xl shadow-xl shadow-black/10 z-[70] overflow-hidden max-h-[200px] overflow-y-auto"
                         >
                              <!-- JS پر میکنه -->
                         </div>
                    </div>

                    <!-- آپلود عکس -->
                    <div>
                         <label class="block text-[11px] font-bold text-gray-500 mb-1.5 mr-1"
                              >تصویر غذا</label
                         >
                         <div class="flex items-center gap-3">
                              <div
                                   id="editImagePreview"
                                   class="w-14 h-14 rounded-full bg-cream-50 border-2 border-dashed border-gray-200 flex items-center justify-center overflow-hidden flex-shrink-0"
                              >
                                   <span class="text-2xl text-gray-300">📷</span>
                              </div>
                              <label
                                   class="cursor-pointer px-4 py-2.5 bg-cream-50 border-2 border-dashed border-gray-200 rounded-xl text-[11px] text-gray-400 font-medium hover:border-brand-400 hover:text-brand-500 hover:bg-brand-50 transition-all"
                              >
                                   انتخاب تصویر
                                   <input
                                        type="file"
                                        name="image"
                                        accept="image/*"
                                        class="hidden"
                                        onchange="previewEditImage(this)"
                                   />
                              </label>
                         </div>
                    </div>

                    <!-- دکمه‌ها -->
                    <div class="flex gap-3 pt-2 pb-4">
                         <button
                              type="submit"
                              class="flex-1 py-3 rounded-xl text-white font-extrabold text-[14px] bg-gradient-to-l from-brand-400 via-brand-500 to-brand-600 hover:shadow-lg hover:shadow-brand-500/30 transition-all duration-300"
                         >
                              <span id="submitBtnText">ذخیره تغییرات</span>
                         </button>
                         <button
                              type="button"
                              onclick="closeEditModal()"
                              class="px-5 py-3 rounded-xl bg-gray-100 text-gray-500 font-bold text-[13px] hover:bg-gray-200 transition-all duration-300"
                         >
                              انصراف
                         </button>
                    </div>
               </form>
          </div>
     </div>
</div>
